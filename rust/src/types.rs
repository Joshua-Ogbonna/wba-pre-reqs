pub fn run() {
    // Boolean
    let is_active = true;

    // Boolean from expression
    let is_greater = 32 > 50;

    println!("{} {}", is_active, is_greater);

    // Char
    let a1 = 'a';
    let face = '😅';
    println!("{} {}", a1, face);
}