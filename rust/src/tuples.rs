pub fn run() {
    let person = ("Joshua", "Imo State", 20);

    println!("{} is from {} and is {}", person.0, person.1, person.2);
}

/*
Tuples group together values of different types
Max 12 elements
*/